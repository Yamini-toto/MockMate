//add (const router = useRouter();) to the top of the file and import { useRouter } from 'next/navigation'; to the imports.
/* if(resp)
    {
      setOpenDialog(false);
      router.push('/dashboard/interview/'+resp[0]?.mockId); add this line to the code later
    }
*/ 